[http.routers]
  [http.routers.dsk-netdata-rtr]
      entryPoints = ["https"]
      rule = "Host(`dsk-netdata.barinohub.com`)"
      service = "dsk-netdata-svc"
      middlewares = ["chain-oauth"]
      [http.routers.dsk-netdata-rtr.tls]
        certresolver = "dns-cloudflare"

[http.services]
  [http.services.dsk-netdata-svc]
    [http.services.dsk-netdata-svc.loadBalancer]
      passHostHeader = true
      [[http.services.dsk-netdata-svc.loadBalancer.servers]]
        url = "http://192.168.1.200:19998"
